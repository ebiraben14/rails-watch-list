<div class="container mt-5">
  <div class="row">
    <div class="col">
      <h1><%= @list.name %></h1>

      <% @list.bookmarks.each do |bookmark| %>
      <p>Title  <strong><%= bookmark.movie.title %></strong><br> Avis <strong><%=bookmark.comment  %></p></strong>   <%= link_to 'detete this Bookmark', bookmark_path(bookmark), method: :delete, class: 'btn btn-cancel', data: { confirm: 'Are you sure you want to delete this list?' } %>
      <% end %>


      <%= link_to  "Add a movie", new_list_bookmark_path(@list), class: 'btn btn-primary mt-3' %>
      <%= link_to "back to Lists", lists_path, class: 'btn btn-primary mt-3'%>

    </div>
  </div>
</div>
